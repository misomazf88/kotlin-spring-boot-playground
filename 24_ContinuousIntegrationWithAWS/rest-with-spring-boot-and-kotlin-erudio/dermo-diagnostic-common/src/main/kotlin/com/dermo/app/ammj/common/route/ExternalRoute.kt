package com.dermo.app.ammj.common.route

object ExternalRoute {

    const val AUTH = "/system-auth/authentication-role"

    object Diagnostic {
        // Example to call future MS
        private const val DIAGNOSTIC = "/create"
    }
}
