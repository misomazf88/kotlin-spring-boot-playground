package com.dermo.app.ammj.core.util

class ProducerUtilTest {

    /*@Test
    fun `should map the whole set of keys`() {
        // Arrange
        val record = Generators.getKafkaProducerRecord()
        val headers = mapOf(
            "key one" to listOf("value 1"),
            "key two" to listOf("value 2")
        )
        // Act
        val result = ProducerUtil.addHeaders(
            headers, record
        )
        // Assert
        Assertions.assertEquals(headers.size, result.headers().count())
        result.headers().forEach {
            Assertions.assertTrue(headers.keys.contains(it.key()))
        }
    }*/
}
